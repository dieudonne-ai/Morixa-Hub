flask==3.0.3
flask-sqlalchemy==3.1.1
flask-cors==4.0.1
# psycopg2-binary ne supporte pas encore Python 3.14 avec wheel précompilé.
# Essayez dans l'ordre jusqu'à ce que l'un fonctionne :
#   pip install psycopg2-binary --only-binary=:all:
#   pip install "psycopg[binary]"   ← psycopg3, recommandé pour Python 3.14
psycopg[binary]>=3.1.0
sqlalchemy==2.0.41
werkzeug==3.0.3
pyjwt==2.8.0
gunicorn==22.0.0
boto3==1.34.131
python-dotenv==1.0.1